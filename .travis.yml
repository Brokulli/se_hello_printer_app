language: python
services:
  - docker
python:
  - "2.7"
install:
  - make deps
script:
  - make test
  - make lint
#after_success:
  - make docker_build
  - make docker_push
deploy:
  provider: heroku
  app: fathomless-ridge-17643 # nazwa naszej aplikacji
  api_key: $(HEROKU_API_KEY) # zmienna (haslo) bedzie pobierana z travisa
  # zakladka settings na travis dodajemy zmienna HEROKU_API_KEY
